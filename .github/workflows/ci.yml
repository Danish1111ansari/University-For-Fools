name: CI/CD pipeline

on: 
  push:
    branches: [ "main" ]
  pull_request: 
    branches: [ "main" ]

jobs: 
 validate-repo:
  runs-on: ubuntu-latest

  steps:
    - name: checkout-code
      uses: actions/checkout@v4

    - name: List Repository files
      uses: ls -R
